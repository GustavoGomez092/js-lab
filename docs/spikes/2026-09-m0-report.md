# M0 Spike Report

| Spike | Question | Result | Decision |
|---|---|---|---|
| S1 | Shell, workspace imports, Worker, paths | Pass in dev and packaged canary (Worker needs its source copied beside the bundle) | go |
| S2 | Monaco workers over views:// | Pass in packaged canary with plain `?worker` imports (no `&inline` fallback needed), once the subpath specifiers matched monaco-editor 0.56.0's `exports` map | go |
| S3 | Bundled Bun child runner | Pass in packaged canary: `process.execPath` spawns with IPC, NODE_PATH resolution, top-level await, no dotenv, SIGKILL. A separately pinned Bun binary also runs and is copied unsigned-by-Hutch, but the default "advanced" IPC serializer breaks across differing Bun minor versions — fixed by explicit `serialization: "json"` | go: `process.execPath` as the M1 runner binary; no separately pinned Bun for now |
| S4 | Embedded webview dialogs + hidden timers | Pass in packaged canary: interval/rAF keep running at 100%/104% of their non-collapsed baseline rate with the webview collapsed to 0x0 for 10s; `alert`/`confirm`/`prompt` inside the embedded webview do NOT block — they return `undefined`/`false`/`null` in ~1ms, confirmed automatically with no human click | go: zero-size (no 1x1 fallback needed); dialogs: §5.12 async-style fallback, not native (proven without a human click) |
| S5 | Info.plist document types + signing | | |
| S6 | osascript save dialog | | |
| S7 | RPC throughput | | |
| S8 | Runtime flags + .npmrc isolation | | |

## Environment

- macOS version: 26.5.2 (build 25F84), Apple Silicon (macos-arm64)
- Electrobun: 2.0.1
- Hutch version: 0.24.3 (the release paired with the `electrobun@2.0.1` npm package)
- Bundled Bun version (from `Bun.version` in main): 1.4.0 (also the Hutch-vendored Bun used when `packageManager: "bun"`)

## Toolchain setup (works as of 2026-09-12)

```bash
# ~/.hutch must be absent or already a marked Hutch home. The npm bootstrap's partial
# cache left an unmarked one, and Hutch refused it: "refusing to claim an unmarked Hutch home".
curl -fsSL https://hutch.blackboard.sh/hutch/install.sh -o install.sh
sh install.sh --version 0.24.3      # appends a PATH line for ~/.hutch/bin to ~/.zshrc
hutch --version                     # 0.24.3

cd spikes/s1-shell
hutch install                       # delegates to the vendored Bun 1.4.0 (packageManager: "bun")
hutch electrobun sync               # projects Electrobun 2.0.1 into .hutch/devkit
hutch run dev                       # dev build + launch
hutch run build                     # canary build -> build/canary-macos-arm64, artifacts/
```

## S1

**Question:** Does an Electrobun 2.0.1 app with a Bun main process start, import workspace packages from main and view, run a Bun `Worker`, and report the expected bundle/userData paths, both in dev and in a packaged canary build?

### Raw probe output

Dev (`hutch run dev`), `~/Library/Application Support/dev.jslab.spike/dev/spike-report.json`:

```json
{"section":"S1","at":"2026-09-13T02:39:28.102Z","data":{"bunVersion":"1.4.0","execPath":"<repo>/spikes/s1-shell/build/dev-macos-arm64/JSLab Spike-dev.app/Contents/MacOS/bun","resourcesFolder":"<repo>/spikes/s1-shell/build/dev-macos-arm64/JSLab Spike-dev.app/Contents/Resources","viewsFolder":"<repo>/spikes/s1-shell/build/dev-macos-arm64/JSLab Spike-dev.app/Contents/Resources/app/views","userData":"~/Library/Application Support/dev.jslab.spike/dev","probeLib":"probe-lib reached from main","worker":{"ok":true,"reply":{"echo":"ping","bun":"1.4.0"}}}}
{"section":"S1-view","at":"2026-09-13T02:39:28.502Z","data":{"probeLib":"probe-lib reached from view"}}
```

Packaged canary (`hutch run build`; .app copied to internal disk, see notes), `~/Library/Application Support/dev.jslab.spike/canary/spike-report.json`:

```json
{"section":"S1","at":"2026-09-13T02:40:10.199Z","data":{"bunVersion":"1.4.0","execPath":"<scratchpad>/canary-app/JSLab Spike-canary.app/Contents/MacOS/bun","resourcesFolder":"<scratchpad>/canary-app/JSLab Spike-canary.app/Contents/Resources","viewsFolder":"<scratchpad>/canary-app/JSLab Spike-canary.app/Contents/Resources/app/views","userData":"~/Library/Application Support/dev.jslab.spike/canary","probeLib":"probe-lib reached from main","worker":{"ok":true,"reply":{"echo":"ping","bun":"1.4.0"}}}}
{"section":"S1-view","at":"2026-09-13T02:40:10.562Z","data":{"probeLib":"probe-lib reached from view"}}
```

### Pass criteria (packaged canary)

| Criterion | Result |
|---|---|
| `probeLib` works from main and view | Pass: `probe-lib reached from main` / `probe-lib reached from view` |
| The worker replied | Pass: `{"echo":"ping","bun":"1.4.0"}` (after the copy-rule fix below) |
| `execPath` inside the `.app` bundle | Pass: `…/JSLab Spike-canary.app/Contents/MacOS/bun` |
| `userData` under `dev.jslab.spike` | Pass: `~/Library/Application Support/dev.jslab.spike/canary` (dev: `…/dev`) |

The window titled "JSLab Spike" rendered the probes JSON in both builds (checked with screenshots).

### Resolved copy destinations

The packaged app uses flat files, not ASAR (no `Resources/app.asar`):

```text
JSLab Spike-canary.app/Contents/Resources/app/
  bun/index.js          <- bundled src/bun/index.ts
  bun/worker.ts         <- copy rule "src/bun/worker.ts": "bun/worker.ts"
  runner/README.txt     <- copy rule runner: "runner"   (runner resolves to Contents/Resources/app/runner)
  views/mainview/       <- dist/index.html + dist/assets
  views/webview-probe/  <- copy rule "webview-probe": "views/webview-probe"
```

At runtime that is `join(PATHS.RESOURCES_FOLDER, "app", "runner")`.

### Findings

1. **Worker needs its file shipped.** Electrobun bundles only `build.bun.entrypoint` into `app/bun/index.js`. Bun's bundler neither emits nor rewrites `new Worker(new URL("./worker.ts", import.meta.url))`, so the worker first timed out in both builds. A standalone Bun 1.4.0 test showed the same `ModuleNotFound`, even with `worker.ts` passed as a second entrypoint, because the emitted file is `worker.js` while the reference stays `./worker.ts`. Fix used here: copy `src/bun/worker.ts` to `bun/worker.ts` and let Bun run the TS directly. That works only for self-contained workers. A worker that imports packages must be pre-bundled with `bun build` into a copied file.
2. **Copy sources must exist.** `hutch electrobun dev/build` fails with `CopySourceMissing` when a `build.copy` source is absent. `runner/` and `webview-probe/` hold placeholder READMEs until S3/S4 fill them.
3. **Packaged canary is a self-extractor.** `build/canary-macos-arm64/JSLab Spike-canary.app` is only a launcher plus `.tar.zst`. On first launch it extracts to `~/Library/Application Support/dev.jslab.spike/canary/`, installs an `uninstall` helper and manifest there, and replaces its own bundle with the real app. It then shows an installer window that waits for a click unless `ELECTROBUN_INSTALLER_UI_AUTOCLOSE=1` is set. Launched from this repo's external volume (`<volume>`), it stalled on a hidden macOS TCC prompt (`kTCCServiceSystemPolicyRemovableVolumes`). Launched from internal disk with auto-close, it ran unattended.
4. **`hutch pm exec` does not work with Bun delegation.** With `packageManager: "bun"`, `hutch pm exec -- vite build` forwards to `bun exec`, which fails with `bun: command not found: vite`. `hutch pm x --no-install vite build` works.
5. **Electrobun must be pinned.** Direct `hutch electrobun sync` floats on the stable channel (currently 2.0.1), so `hutch.config.ts` pins `electrobun: { version: "2.0.1" }`.
6. `tsc --noEmit` reports one error, inside the generated devkit (`WindowsUpdateTask.ts`: `AggregateError` is missing under the template's `lib: ES2020`). The spike sources themselves typecheck cleanly.

### System changes

- **`~/.zshrc`:** the Hutch installer (`install.sh --version 0.24.3`) appended a PATH block. Exact diff against a backup taken just before install, with the home directory written as `~` (the file itself contains the absolute home path):

  ```diff
  128a129,131
  >
  > # Hutch
  > export PATH='~/.hutch/bin':"$PATH"
  ```

- **`~/.hutch` deleted and recreated.** Before this task, an earlier `bunx electrobun@2.0.1` probe had left `~/.hutch` holding only `npm/`, `releases/` and `state/locks/`, with no Hutch store marker. Hutch refused to use it (`hutch: refusing to claim an unmarked Hutch home that is a user/root directory or contains unrelated files`). That probe had created the directory and nothing else used it, so it was deleted. It was then recreated by installing exactly Hutch 0.24.3, the release `electrobun@2.0.1` requires: its npm bootstrap rejects any global Hutch that doesn't resolve to 0.24.3, so the latest 0.26.0 fails. It now holds `bin/hutch`, `releases/{hutch/0.24.3, cottontail/0.5.0, electrobun/2.0.1}`, `toolchains/bun/1.4.0`, `state/` and `cache/`.
- **Other writes:** `~/Library/Application Support/dev.jslab.spike/{dev,canary}` (app userData; `canary/` also holds the extractor's `self-extraction/` tar, `uninstall` helper and `.electrobun-uninstall.json`), `~/.cache/cottontail` (Hutch build-time runtime cache), and `~/.bun/install/cache` (Bun package cache). `~/.npmrc` and other dotfiles are untouched.

### Manual checks pending (user)

- Double-click `spikes/s1-shell/build/canary-macos-arm64/JSLab Spike-canary.app` in Finder to confirm the normal first-launch flow. Expect a macOS prompt for access to files on a removable volume (the repo is on `<volume>`) and an installer panel that needs a Close click. After that, the "JSLab Spike" window should show the probes JSON.
- This check is supplementary. The S1 decision does not depend on it: every S1 pass criterion was verified automatically in the packaged canary build above.

### Decision

**go:** use workspace packages directly. `@spike/probe-lib` (exported as raw `.ts` through `workspace:*`) resolved in both the Bun main bundle and the Vite view bundle, in dev and in the packaged canary build, with no pre-bundling step. Bun Workers work in the packaged app as long as the worker file ships beside `app/bun/index.js`: a copy rule for self-contained TS workers, or a pre-built bundle for workers with imports.

## S2

**Question:** Do Monaco's editor and TypeScript web workers load inside the packaged app's WKWebView over `views://`?

### Implementation

- Added `monaco-editor@0.56.0` to `spikes/s1-shell/package.json` via `hutch pm add monaco-editor@0.56.0`.
- Added `spikes/s1-shell/src/mainview/MonacoProbe.tsx`, rendered below the `<pre>` in `App.tsx`, matching the brief with one required import fix (see Deviations).
- Added `spikes/s1-shell/src/mainview/vite-env.d.ts` (`/// <reference types="vite/client" />`), not listed in the brief's file list, so `tsc --noEmit` recognizes Vite's `?worker` import suffix. Without it, `?worker` imports type-check as "Cannot find module" (TS2307) even though the Vite/Rollup build itself does not need the ambient type — this only mattered for a clean `tsc --noEmit`, not for the runtime pass criteria.

### Deviations from the brief

1. **`rpc` import path.** Per Task 1's finding (R2 ruling), `rpc` lives in `./rpc`, not `./App`. `MonacoProbe.tsx` imports `{ rpc } from "./rpc"` instead of `"./App"` as literally written in the brief.
2. **Worker subpath specifiers.** The brief's exact import paths, `monaco-editor/esm/vs/editor/editor.worker?worker` and `monaco-editor/esm/vs/languages/features/typescript/ts.worker?worker`, fail to resolve at Vite/Rollup build time (not just at runtime): `[vite]: Rollup failed to resolve import "monaco-editor/esm/vs/editor/editor.worker?worker"`. Root cause: `monaco-editor@0.56.0`'s `package.json` `exports` map is `"./*": "./esm/vs/*.js"` — the wildcard target already prepends `esm/vs/`. Requesting `monaco-editor/esm/vs/editor/editor.worker` matches the pattern with `* = "esm/vs/editor/editor.worker"`, and substituting into the target doubles the prefix to `./esm/vs/esm/vs/editor/editor.worker.js`, which does not exist (confirmed via `require.resolve` reproducing the identical doubled path and via inspecting `node_modules/monaco-editor/package.json`). Fix: drop the redundant `esm/vs/` prefix from the specifiers, giving `monaco-editor/editor/editor.worker?worker` and `monaco-editor/languages/features/typescript/ts.worker?worker`, which correctly resolve through the same `"./*"` pattern to `esm/vs/editor/editor.worker.js` and `esm/vs/languages/features/typescript/ts.worker.js`. This fix is exports-map plumbing, not the `?worker&inline` fallback described in the brief's Step 5 — the plain (non-inline) `?worker` variant passed once the specifiers were corrected, so Step 5's fallback was not needed.
3. `monaco.typescript.typescriptDefaults` / `monaco.typescript.getTypeScriptWorker()` as written in the brief were verified to be correct against the installed `monaco-editor@0.56.0` typings (a top-level `typescript` namespace re-exported from `esm/vs/index.d.ts`, distinct from the deprecated `monaco.languages.typescript` stub) — no change needed here, noted only because it looked suspicious before checking.

### Raw probe output

Packaged canary (`hutch run build`; `.app` copied to internal disk scratchpad and launched with `ELECTROBUN_INSTALLER_UI_AUTOCLOSE=1`, same procedure as S1), `~/Library/Application Support/dev.jslab.spike/canary/spike-report.json`:

```json
{"section":"S2","at":"2026-09-13T02:53:29.914Z","data":{"location":"views://mainview/index.html","markerMessages":["Type 'string' is not assignable to type 'number'.","Property 'ma' does not exist on type 'number[]'."],"completionHasMap":true}}
```

### Pass criteria (packaged canary, plain `?worker` variant)

| Criterion | Result |
|---|---|
| `location` starts with `views://` | Pass: `views://mainview/index.html` |
| `markerMessages` contains `Type 'string' is not assignable to type 'number'.` | Pass |
| `completionHasMap` is `true` | Pass: `true` |
| Manual: typing `[1, 2].` shows a suggestion list; hovering `x` shows `const x: number` | Not automated — see below |

All three automated (binding, per controller ruling R1) criteria passed on the first variant tried (plain `?worker`, once the subpath specifiers were fixed per Deviation 2). The `?worker&inline` fallback (brief Step 5) was not exercised because it was not needed.

### Manual checks pending (user)

- Typing `[1, 2].` in the probe editor should show a suggestion list including `map`; hovering `x` should show a `const x: number` type tooltip. Not automated: Monaco's hover/suggest UI requires simulated keyboard/mouse interaction inside the WKWebView, which this probe harness (RPC-only reporting) does not drive. Per ruling R1, the S2 decision does not depend on this: the completions API call in the probe (`getCompletionsAtPosition`, checked for a `map` entry) exercises the same TypeScript worker path a real suggestion widget would use, and `completionHasMap: true` in the raw data above already demonstrates the underlying data is correct.

### System changes

None beyond what S1 already recorded. `monaco-editor@0.56.0` was added to `spikes/s1-shell`'s own `node_modules` (gitignored) and `bun.lock`; no changes to `~/.hutch`, `~/.zshrc`, or global Hutch/Bun state.

### Decision

**go:** Monaco's editor and TypeScript web workers load and run inside the packaged app's WKWebView over `views://`, using plain (non-inline) Vite `?worker` imports — no `&inline` fallback required. M1 Task 17 should use:

```ts
import EditorWorker from "monaco-editor/editor/editor.worker?worker";
import TsWorker from "monaco-editor/languages/features/typescript/ts.worker?worker";
```

Note these subpath specifiers omit the `esm/vs/` segment that appears in the on-disk file paths — `monaco-editor@0.56.0`'s `exports` map (`"./*": "./esm/vs/*.js"`) already prepends it, so including it in the specifier resolves to a doubled, nonexistent path (see Deviation 2). If a future `monaco-editor` version changes its `exports` map, re-derive the specifier from that map rather than reusing the on-disk path.

## S3

**Question:** Can the packaged app's main process spawn its bundled Bun binary as a child process with IPC, `NODE_PATH` package resolution, top-level await, disabled `.env` auto-loading, and `SIGKILL`, and should JSLab ship a separately pinned Bun binary for runners (risk R9)?

### Implementation

- `spikes/s1-shell/runner/child.mjs`: the IPC child script from the brief verbatim — sends `ready` with `Bun.version`/`process.execPath`, then on a `run` message resolves `fixture` from `NODE_PATH`, awaits a top-level `Promise.resolve(42)`, and reports `process.env.SPIKE_DOTENV` and `process.cwd()`.
- `spikes/s1-shell/runner/fixture-pkg/node_modules/fixture/{package.json,index.js}`: the fixture package resolved only via `NODE_PATH` (not a real dependency), verbatim from the brief.
- `spikes/s1-shell/src/bun/index.ts`: added `s3Probe()` (see Deviations for the structural change from the brief's literal code) and `writeReport("S3", await s3Probe())`.
- Step 5 (R9): downloaded Bun 1.3.13 darwin-aarch64, unzipped `bun-darwin-aarch64/bun` to `spikes/s1-shell/runner/bun-bin/bun`, added a second (then third) spawn in `s3Probe` using that path instead of `process.execPath`.

### Deviations from the brief

1. **Restructured `s3Probe` to run both binaries concurrently and share one spawn helper (`runBinary`)**, instead of the brief's single flat probe body. Rationale: Step 5 asks for "a second spawn... instead of process.execPath," and each spawn's own `await Bun.sleep(3000)` would serialize to 6s+ (then 9s once a third JSON-serialization run was added for diagnosis, see Deviation 3) if run sequentially, eating most of the verification window and slowing every rebuild/relaunch cycle. Field names differ from the brief's flat `out.readyMs`/`out.messages`/`out.exitCode`/`out.stderr`/`out.execPathCodesign`: they now live under `out.execPathRun.{readyMs,messages,exitCode,stderr,codesign}` and `out.pinnedBunRun.{...}` (same shape, one level deeper). All brief pass-criteria data is present, just at these paths.
2. **Removed `spikes/s1-shell/runner/README.txt`** (Task 1's placeholder) instead of keeping it, since `runner/` now holds real files and the copy rule no longer needs a placeholder to resolve.
3. **Added a third spawn, `pinnedBunJsonRun`**, not in the brief. The plain second spawn (`pinnedBunRun`, brief Step 5) crashed on the `run` IPC message with `TypeError: Unable to deserialize data.` — Bun's default "advanced" (V8 structured-clone) IPC serializer is not wire-compatible between the main process's Bun 1.4.0 and a separately pinned Bun 1.3.13 child. Per "when in over your head" — this is a genuine diagnostic follow-up, not scope creep — I re-ran the pinned binary with `Bun.spawn`'s `serialization: "json"` option to check whether an explicit stable wire format sidesteps the version-skew break. It did (see raw data). This finding materially changes the R9 answer, so it is included rather than left as an unexplained failure.
4. **`.gitignore` gotcha:** the existing blanket `node_modules/` rule in `spikes/s1-shell/.gitignore` also silently ignored `runner/fixture-pkg/node_modules/fixture/`, which the brief requires to be committed (git directory-exclusion means a nested `!pattern` cannot resurrect it). Rather than punch a hole in the blanket rule, the fixture files were `git add -f`'d directly; `.gitignore` itself only gained the new `runner/bun-bin/` entry (R3, the downloaded Bun 1.3.13 binary, ~60 MB, is not committed).

### Raw probe output

Packaged canary (`hutch run build`; `.app` copied to internal disk scratchpad and launched with `ELECTROBUN_INSTALLER_UI_AUTOCLOSE=1`, same procedure as S1/S2), `~/Library/Application Support/dev.jslab.spike/canary/spike-report.json`, `S3` line (reformatted for readability; `codesign`/`stderr` strings kept verbatim):

Abbreviated for readability:

```json
{
  "section": "S3",
  "data": {
    "childPath": ".../Contents/Resources/app/runner/child.mjs",
    "childExists": true,
    "pinnedBunPath": ".../Contents/Resources/app/runner/bun-bin/bun",
    "pinnedBunExists": true,
    "execPathRun": {
      "bin": ".../Contents/MacOS/bun",
      "serialization": "advanced",
      "readyMs": 10,
      "exitCode": 137,
      "messages": [
        { "type": "ready", "bun": "1.4.0", "execPath": ".../Contents/MacOS/bun" },
        { "type": "result", "fixture": "fixture resolved via NODE_PATH", "tla": 42, "dotenv": null, "cwd": ".../dev.jslab.spike/canary/s3-cwd" }
      ],
      "stderr": "",
      "codesign": "...TeamIdentifier=7FRXF46ZSN\nRuntime Version=26.5.0..."
    },
    "pinnedBunRun": {
      "bin": ".../Contents/Resources/app/runner/bun-bin/bun",
      "serialization": "advanced",
      "readyMs": 24,
      "exitCode": 1,
      "messages": [
        { "type": "ready", "bun": "1.3.13", "execPath": ".../Contents/Resources/app/runner/bun-bin/bun" }
      ],
      "stderr": "TypeError: Unable to deserialize data.\n\nBun v1.3.13 (macOS arm64)\n",
      "codesign": "...TeamIdentifier=7FRXF46ZSN\nRuntime Version=15.2.0..."
    },
    "pinnedBunJsonRun": {
      "bin": ".../Contents/Resources/app/runner/bun-bin/bun",
      "serialization": "json",
      "readyMs": 19,
      "exitCode": 137,
      "messages": [
        { "type": "ready", "bun": "1.3.13", "execPath": ".../Contents/Resources/app/runner/bun-bin/bun" },
        { "type": "result", "fixture": "fixture resolved via NODE_PATH", "tla": 42, "dotenv": null, "cwd": ".../dev.jslab.spike/canary/s3-cwd" }
      ],
      "stderr": "",
      "codesign": "...TeamIdentifier=7FRXF46ZSN\nRuntime Version=15.2.0..."
    }
  }
}
```

**Full, untruncated line** (this is the whole committed evidentiary record for this probe run — it is NOT otherwise preserved anywhere else in the repo or in git history; the running app's `spike-report.json` lives under `~/Library/Application Support/dev.jslab.spike/canary/`, outside the repo, and is not committed. Home directory paths below are written as `~`; all other absolute paths, including the session scratchpad path the canary `.app` was copied to and launched from, are left exactly as produced):

```json
{"section":"S3","at":"2026-09-13T03:04:42.858Z","data":{"childPath":"<scratchpad>/canary-app/JSLab Spike-canary.app/Contents/Resources/app/runner/child.mjs","childExists":true,"pinnedBunPath":"<scratchpad>/canary-app/JSLab Spike-canary.app/Contents/Resources/app/runner/bun-bin/bun","pinnedBunExists":true,"execPathRun":{"bin":"<scratchpad>/canary-app/JSLab Spike-canary.app/Contents/MacOS/bun","serialization":"advanced","readyMs":10,"exitCode":137,"messages":[{"type":"ready","bun":"1.4.0","execPath":"<scratchpad>/canary-app/JSLab Spike-canary.app/Contents/MacOS/bun"},{"type":"result","fixture":"fixture resolved via NODE_PATH","tla":42,"dotenv":null,"cwd":"~/Library/Application Support/dev.jslab.spike/canary/s3-cwd"}],"stderr":"","codesign":"Executable=<scratchpad>/canary-app/JSLab Spike-canary.app/Contents/MacOS/bun\nIdentifier=bun\nFormat=Mach-O thin (arm64)\nCodeDirectory v=20500 size=124207 flags=0x10000(runtime) hashes=3871+7 location=embedded\nSignature size=8975\nTimestamp=Aug 19, 2026 at 6:23:47 PM\nInfo.plist=not bound\nTeamIdentifier=7FRXF46ZSN\nRuntime Version=26.5.0\nSealed Resources=none\nInternal requirements count=1 size=164\n"},"pinnedBunRun":{"bin":"<scratchpad>/canary-app/JSLab Spike-canary.app/Contents/Resources/app/runner/bun-bin/bun","serialization":"advanced","readyMs":24,"exitCode":1,"messages":[{"type":"ready","bun":"1.3.13","execPath":"<scratchpad>/canary-app/JSLab Spike-canary.app/Contents/Resources/app/runner/bun-bin/bun"}],"stderr":"TypeError: Unable to deserialize data.\n\nBun v1.3.13 (macOS arm64)\n","codesign":"Executable=<scratchpad>/canary-app/JSLab Spike-canary.app/Contents/Resources/app/runner/bun-bin/bun\nIdentifier=bun\nFormat=Mach-O thin (arm64)\nCodeDirectory v=20500 size=489135 flags=0x10000(runtime) hashes=15275+7 location=embedded\nSignature size=8976\nTimestamp=Apr 19, 2026 at 12:56:17 AM\nInfo.plist=not bound\nTeamIdentifier=7FRXF46ZSN\nRuntime Version=15.2.0\nSealed Resources=none\nInternal requirements count=1 size=164\n"},"pinnedBunJsonRun":{"bin":"<scratchpad>/canary-app/JSLab Spike-canary.app/Contents/Resources/app/runner/bun-bin/bun","serialization":"json","readyMs":19,"exitCode":137,"messages":[{"type":"ready","bun":"1.3.13","execPath":"<scratchpad>/canary-app/JSLab Spike-canary.app/Contents/Resources/app/runner/bun-bin/bun"},{"type":"result","fixture":"fixture resolved via NODE_PATH","tla":42,"dotenv":null,"cwd":"~/Library/Application Support/dev.jslab.spike/canary/s3-cwd"}],"stderr":"","codesign":"Executable=<scratchpad>/canary-app/JSLab Spike-canary.app/Contents/Resources/app/runner/bun-bin/bun\nIdentifier=bun\nFormat=Mach-O thin (arm64)\nCodeDirectory v=20500 size=489135 flags=0x10000(runtime) hashes=15275+7 location=embedded\nSignature size=8976\nTimestamp=Apr 19, 2026 at 12:56:17 AM\nInfo.plist=not bound\nTeamIdentifier=7FRXF46ZSN\nRuntime Version=15.2.0\nSealed Resources=none\nInternal requirements count=1 size=164\n"}}}
```

### Pass criteria (packaged canary, `process.execPath`, brief Steps 3-4)

| Criterion | Result |
|---|---|
| `childExists` is `true` | Pass |
| `messages` contains `ready` with `execPath` inside the bundle | Pass: `.../JSLab Spike-canary.app/Contents/MacOS/bun` |
| `messages` contains a `result` with `fixture: "fixture resolved via NODE_PATH"`, `tla: 42`, `dotenv: null` | Pass |
| `readyMs` under 300 | Pass: `10` |
| `exitCode` non-null (the kill worked) | Pass: `137` (128 + SIGKILL) |

### R9 evaluation (Step 5): separately pinned Bun 1.3.13

| Check | Result |
|---|---|
| Copied binary exists at `runner/bun-bin/bun` in the packaged app | Pass: `pinnedBunExists: true` |
| Runs, reports `ready` | Pass: `ready` at `readyMs: 24` (advanced) / `19` (json), reporting `"bun": "1.3.13"` |
| Signed by Hutch's nested signing | **No.** Compare the `pinnedBunRun.codesign` / `pinnedBunJsonRun.codesign` value in the full raw line above against the pre-build check run directly on the unzipped download, `codesign -dv spikes/s1-shell/runner/bun-bin/bun`, output captured below verbatim. Every field is identical except the `Executable=` path itself (source-repo path vs. the path inside the copied `.app`) — same `CodeDirectory` size (`489135`), same hash count (`15275+7`), same `Signature size` (`8976`), same `Timestamp` (`Apr 19, 2026 at 12:56:17 AM`), same `TeamIdentifier` (`7FRXF46ZSN`), same `Runtime Version` (`15.2.0`). Hutch's build/copy step does not re-sign loose files under `Resources/app/runner/`; the binary ships with its original upstream Developer ID signature (same team as the bundled Bun, `7FRXF46ZSN` — Oven's own identity, not Hutch's), hardened runtime flag intact:<br><br>`Executable=<repo>/spikes/s1-shell/runner/bun-bin/bun`<br>`Identifier=bun`<br>`Format=Mach-O thin (arm64)`<br>`CodeDirectory v=20500 size=489135 flags=0x10000(runtime) hashes=15275+7 location=embedded`<br>`Signature size=8976`<br>`Timestamp=Apr 19, 2026 at 12:56:17 AM`<br>`Info.plist=not bound`<br>`TeamIdentifier=7FRXF46ZSN`<br>`Runtime Version=15.2.0`<br>`Sealed Resources=none`<br>`Internal requirements count=1 size=164` |
| IPC to a differently-versioned child works | **Conditional.** Default (`serialization: "advanced"`, Bun's V8-structured-clone wire format): **fails** — the `run` message crashes the 1.3.13 child with `TypeError: Unable to deserialize data.` before it can reply, main process observes `exitCode: 1` (crash) rather than the SIGKILL's `137`. With `serialization: "json"` explicitly set on `Bun.spawn`'s `ipc` option: **passes**, identical `ready`/`result` messages and `dotenv`/`tla`/`fixture` values as the `process.execPath` run, `exitCode: 137`. |

### Dotenv auto-load control (fix round 1)

`dotenv: null` in the main `S3` probe only shows `SPIKE_DOTENV` was unset — it does not by itself prove `--no-env-file` is *why*, since there was no run without the flag to compare against. JSLab's M1 depends on this flag actually disabling `.env` auto-loading, so a control was added: the same `child.mjs`, the same `cwd` containing `.env` (`SPIKE_DOTENV=should-not-load`), the same `NODE_PATH`, spawned with the packaged canary's own bundled Bun (`process.execPath`, the binary M1 will use per the Decision below) — once with `--no-env-file` and once without.

This did not require a rebuild: a standalone script (`s3-dotenv-control.mjs`, not committed — a one-off diagnostic, not part of the probe's pass criteria) was run directly with the already-built canary app's bundled Bun binary, copied to the session scratchpad in the S3/S4 verification steps above:

```bash
"$SCRATCH/canary-app/JSLab Spike-canary.app/Contents/MacOS/bun" \
  s3-dotenv-control.mjs \
  "$SCRATCH/canary-app/JSLab Spike-canary.app/Contents/Resources/app" \
  "$SCRATCH/s3-dotenv-cwd"
```

The script (full text, for reproducibility):

```js
import { mkdirSync, writeFileSync } from "node:fs";
import { join } from "node:path";

const appDir = process.argv[2];
const cwd = process.argv[3];
mkdirSync(cwd, { recursive: true });
writeFileSync(join(cwd, ".env"), "SPIKE_DOTENV=should-not-load\n");
const childPath = join(appDir, "runner", "child.mjs");
const nodePath = join(appDir, "runner", "fixture-pkg", "node_modules");

async function run(useFlag) {
  const args = useFlag ? [process.execPath, "--no-env-file", childPath] : [process.execPath, childPath];
  const messages = [];
  const child = Bun.spawn(args, {
    cwd,
    env: { ...process.env, NODE_PATH: nodePath },
    stderr: "pipe",
    ipc(message) {
      messages.push(message);
      if (message.type === "ready") child.send({ type: "run" });
    },
  });
  await Bun.sleep(1500);
  child.kill("SIGKILL");
  const exitCode = await child.exited;
  const stderr = await new Response(child.stderr).text();
  return { useFlag, args, messages, exitCode, stderr };
}

const withFlag = await run(true);
const withoutFlag = await run(false);
console.log(JSON.stringify({ withFlag, withoutFlag }, null, 2));
```

Raw output (full, untruncated — scratchpad paths left as produced, no home-directory paths appear in this output):

```json
{
  "withFlag": {
    "useFlag": true,
    "args": [
      "<scratchpad>/canary-app/JSLab Spike-canary.app/Contents/MacOS/bun",
      "--no-env-file",
      "<scratchpad>/canary-app/JSLab Spike-canary.app/Contents/Resources/app/runner/child.mjs"
    ],
    "messages": [
      { "type": "ready", "bun": "1.4.0", "execPath": "<scratchpad>/canary-app/JSLab Spike-canary.app/Contents/MacOS/bun" },
      { "type": "result", "fixture": "fixture resolved via NODE_PATH", "tla": 42, "dotenv": null, "cwd": "<scratchpad>/s3-dotenv-cwd" }
    ],
    "exitCode": 137,
    "stderr": ""
  },
  "withoutFlag": {
    "useFlag": false,
    "args": [
      "<scratchpad>/canary-app/JSLab Spike-canary.app/Contents/MacOS/bun",
      "<scratchpad>/canary-app/JSLab Spike-canary.app/Contents/Resources/app/runner/child.mjs"
    ],
    "messages": [
      { "type": "ready", "bun": "1.4.0", "execPath": "<scratchpad>/canary-app/JSLab Spike-canary.app/Contents/MacOS/bun" },
      { "type": "result", "fixture": "fixture resolved via NODE_PATH", "tla": 42, "dotenv": "should-not-load", "cwd": "<scratchpad>/s3-dotenv-cwd" }
    ],
    "exitCode": 137,
    "stderr": ""
  }
}
```

**Finding:** `--no-env-file` is proven necessary and sufficient here. Without it, the bundled Bun auto-loads the `cwd`'s `.env` and `dotenv` comes back `"should-not-load"` — the exact string written to the file. With it, `dotenv` comes back `null`. The flag is real and load-bearing, not a no-op against an already-disabled default; M1's runner must pass it explicitly.

### Manual checks pending (user)

None. Every Step 3/4 pass criterion, the Step 5 R9 checks, and the dotenv control above were verified automatically from committed/captured probe output; nothing here needs a human click-through. (Per ruling R1, this section is present because it applies, not omitted.)

### System changes

- Downloaded `bun-darwin-aarch64.zip` (Bun 1.3.13, ~22 MB compressed / 63,072,928 bytes uncompressed) to the session scratchpad and unzipped `bun-darwin-aarch64/bun` to `spikes/s1-shell/runner/bun-bin/bun` (gitignored, not committed, per R3):

  ```bash
  curl -fL -o "$SCRATCHPAD/bun-1.3.13-download/bun-darwin-aarch64.zip" \
    https://github.com/oven-sh/bun/releases/download/bun-v1.3.13/bun-darwin-aarch64.zip
  unzip -o -j "$SCRATCHPAD/bun-1.3.13-download/bun-darwin-aarch64.zip" "bun-darwin-aarch64/bun" \
    -d spikes/s1-shell/runner/bun-bin
  chmod +x spikes/s1-shell/runner/bun-bin/bun
  ```

- No other system changes beyond what S1 already recorded. Each rebuild's packaged canary was copied to `$SCRATCHPAD/canary-app/` and launched with `ELECTROBUN_INSTALLER_UI_AUTOCLOSE=1`; `~/Library/Application Support/dev.jslab.spike/canary/` was wiped before each relaunch (both the `spike-report.json` and the extractor's `self-extraction/`/`uninstall`/`.electrobun-uninstall.json`) to force a fresh self-extraction, since the extractor's manifest pins `app_bundle_path` to the scratchpad path from a previous S1/S2 run and would otherwise skip re-extracting an unchanged `version: "0.0.1"`. The app was quit (`osascript ... quit`, then `pkill -f` as a fallback) after each check.

### Decision

**go:** M1's runner spawns the bundled Bun via `process.execPath` — same binary as the main process, same version, no separate download, no separate signing/notarization story, and no IPC-serialization coupling to worry about since both ends are always the same Bun build. This is `appPaths.runnerBootstrap()`'s answer for "which binary": `process.execPath`, not a bundled or pinned alternative. The dotenv control above confirms `--no-env-file` is a required argument, not optional hardening: omitting it lets a `.env` in the run's `cwd` leak into the executed script's `process.env`.

**Copied-file path formula:** `join(PATHS.RESOURCES_FOLDER, "app", "runner", …)` — confirmed again here (`childPath`, `pinnedBunPath` both resolved and existed at this formula in the packaged canary), consistent with S1.

**R9 (separately pinned runner Bun): no, not for M0/M1.** A separately pinned Bun *can* work — it runs, and Hutch does not interfere with or re-sign it — but the default IPC wire format is not safe across Bun versions that differ from whatever `process.execPath` happens to be, and that gap only shows up at runtime (a silent-until-invoked child crash, not a build or launch failure). Shipping one would add ~63 MB, a separate download/update/notarization-verification story, and a standing requirement to always pass `serialization: "json"` on every runner IPC channel to stay safe against future version drift — for no capability `process.execPath` doesn't already provide at M0/M1's scope. Revisit only if a concrete feature gap appears between Electrobun's bundled Bun and a newer Bun release (e.g., a runner-only API JSLab needs before Electrobun catches up); if that happens, `serialization: "json"` is the required mitigation, not optional hardening.

## S4

**Question:** Inside an embedded `<electrobun-webview>`, do `alert`/`confirm`/`prompt` show native blocking dialogs that return real user-chosen values, and do timers (`setInterval`) and `requestAnimationFrame` keep running when that embedded webview is collapsed to zero size? This decides M4's browser-runtime approach to dialogs and to hidden/background tabs.

### Step 1: tag docs at the pinned version

Read at the pinned-tag clone (ruling R3; the brief's literal `/tmp/electrobun-2.0.1` does not exist here): `.../electrobun-2.0.1/docs/src/content/docs/electrobun/apis/browser/electrobun-webview-tag.mdx` and `.../guides/architecture/webview-tag.mdx`.

- **URL attribute:** `src` (matches the brief; no change needed). Also documented: `html` (inline document), `preload` (JS evaluated before the embedded page's scripts).
- **Partition/sandbox:** `partition` (string, persistent session/storage partition name) and `sandbox` (boolean attribute) both exist as documented and match the names the brief assumed. `sandbox` "disables Electrobun RPC in the child while retaining events and navigation controls" — the event bridge (`host-message` / `window.__electrobunSendToHost`) explicitly "remains available in sandbox mode." The probe does not set `sandbox` (it needs no app RPC in the embedded page; using it unsandboxed most closely matches a "just call alert() and see what happens" test, and the host-message bridge would work identically either way).
- **Host⇄embedded messaging API:**
  - Embedded → host: `window.__electrobunSendToHost(value)` is injected into *every* Electrobun-managed webview's global scope before application scripts run (`docs/.../apis/browser/global-properties.mdx`) — no import or preload attribute needed. It emits `value` as a `host-message` CustomEvent on the containing `<electrobun-webview>` element, delivered to `event.detail`.
  - Host → embedded: `webviewEl.executeJavascript(js: string): void` (confirmed against the projected devkit's type, `.hutch/devkit/api/browser/webviewtag.ts`) runs arbitrary JS inside the embedded page. It is fire-and-forget (`void`, no return value/promise) — a reply, if wanted, has to come back over the `host-message` bridge above.
  - Host listens with `webviewEl.on("host-message", (event: CustomEvent) => ...)` / `.off(...)`.

This is a richer, two-way channel than the brief anticipated (which only planned a human running `spikeTicks()` in Web Inspector). It is what makes the ruling R1 automation possible: the host can both *push* code into the embedded page on demand and *receive* pushes back from it, with no human involved.

### Implementation

- `spikes/s1-shell/webview-probe/index.html`: the brief's page, extended (not replaced) with:
  - `sendTicksNow()` (also run every 200ms by its own `setInterval`) posts `{ type: "ticks", intervalTicks, rafTicks, at }` to the host via `window.__electrobunSendToHost`.
  - `runDialogsTest()` is the brief's button handler, factored into a named function assigned to both `window.runDialogsTest` (so the host can invoke it automatically via `executeJavascript`) and the button's `onclick` (so a human can still trigger it manually). It also posts `dialog-start` before, and a `dialog-result` message (mirroring the on-page `log()` output) after, the three dialog calls.
  - `window.spikeTicks()` is kept verbatim for a human who does open Web Inspector.
- `spikes/s1-shell/src/mainview/WebviewProbe.tsx`: renders the `<electrobun-webview>` per the brief, but instead of a manual "Collapse for 10s" button (ruling R1: this harness cannot click GUI buttons), a `useEffect` on mount automatically runs, in order: (1) a 10s non-collapsed baseline window, (2) a 10s zero-size-collapsed window, (3) — only if the interval kept under 80% of its ticks during (2) — a further 10s 1×1-offscreen window, (4) the dialogs probe, triggered via `executeJavascript("window.runDialogsTest()")` only after (1)–(3) finish, so a blocking dialog can't corrupt the timer measurement. Every phase reports through `rpc.send.viewReport` (sections `S4-collapse`, `S4-timers`, `S4-timers-1x1` (not needed here), `S4-dialogs-trigger`, `S4-dialogs`). Tick snapshots are taken by calling `executeJavascript("window.sendTicksNow()")` and reading the next `host-message` (`forceRead()`), then diffing two cumulative-counter snapshots — this stays correct regardless of whether messages are delivered live during a collapse or only once queued/replayed after restoring, so it doesn't depend on delivery being real-time.
- `spikes/s1-shell/src/mainview/App.tsx`: renders `<WebviewProbe />` below `<MonacoProbe />`.
- Removed `spikes/s1-shell/webview-probe/README.txt` (S1's placeholder), same rationale as S3's removal of `runner/README.txt`: the copy rule now has real content to copy.

### Deviations from the brief

1. **No manual "Collapse webview for 10s" button; the whole sequence runs automatically on mount.** Ruling R1 requires automating what the brief's Step 4 does by hand. `WebviewProbe.tsx` therefore does not match the brief's literal code (which used a `collapsed` state + button + `rpc.send.viewReport({ section: "S4-collapse", data: { startedAt } })` only on click); the same report shape is sent, just triggered by the effect instead of a click.
2. **Dialogs triggered via `executeJavascript`, not the button, and only after the timer probe.** Also ruling R1 (no GUI clicks available). Sequencing them after the 20–30s timer probe, rather than concurrently, avoids a blocking `alert()` freezing the interval/rAF counters mid-measurement. The button remains wired for a human to re-run the same test manually.
3. **Added a host↔embedded push/pull messaging layer** (`sendTicksNow`/`host-message`) not in the brief's page, which only exposed `window.spikeTicks()` for manual Web Inspector use. Required because ruling R1 forbids relying on Web Inspector.
4. **`ref` typed as `HTMLElement` then cast to `WebviewTagElement` inside the effect**, rather than importing `WebviewTagElement` for the `useRef` type parameter directly, to keep the JSX `ref` prop assignment simple; `import type { WebviewTagElement } from "electrobun/view"` is still used for the cast.

### Raw probe output

Packaged canary (`hutch run build`; `.app` copied to internal disk scratchpad and launched with `ELECTROBUN_INSTALLER_UI_AUTOCLOSE=1`, same procedure as S1–S3), `~/Library/Application Support/dev.jslab.spike/canary/spike-report.json`, S4 lines (full, untruncated; no home-directory paths appear in these particular lines). **This replaces an earlier run's numbers — see "Fix round 1" below for why:**

```json
{"section":"S4-collapse","at":"2026-09-13T03:38:11.328Z","data":{"startedAt":1789270691312,"variant":"zero-size"}}
{"section":"S4-timers","at":"2026-09-13T03:38:21.949Z","data":{"variant":"zero-size","expectedInterval":100,"expectedRaf":600,"baselineElapsedMs":10086,"baselineIntervalTicks":97,"baselineRafTicks":1075,"baselineIntervalRatePerSec":9.617291294864168,"baselineRafRatePerSec":106.5833829069998,"collapseElapsedMs":10105,"intervalTicksDuringCollapse":97,"rafTicksDuringCollapse":1118,"intervalRatePerSecDuringCollapse":9.599208312716478,"rafRatePerSecDuringCollapse":110.63829787234042,"pctOfBaselineIntervalRate":100,"pctOfBaselineRafRate":104,"midCollapseAttempt":{"type":"ticks","intervalTicks":154,"rafTicks":1741,"at":1789270696335},"collapseStart":{"type":"ticks","intervalTicks":106,"rafTicks":1170,"at":1789270691260},"collapseEnd":{"type":"ticks","intervalTicks":203,"rafTicks":2288,"at":1789270701365}}}
{"section":"S4-dialogs-trigger","at":"2026-09-13T03:38:21.998Z","data":{"at":1789270701984,"beforeDialog":{"type":"ticks","intervalTicks":208,"rafTicks":2354,"at":1789270701932}}}
{"section":"S4-dialogs","at":"2026-09-13T03:38:22.202Z","data":{"resumedAutomatically":true,"dialogTriggerAt":1789270701984,"type":"dialog-result","alertBlockedMs":1,"confirmed":false,"prompted":null,"at":1789270701986}}
```

`collapseStart`/`collapseEnd` are now both read while the webview is still the size the window describes (the baseline window never resizes anything; the collapse window's `collapseEnd` is read immediately before `restore()` runs — see Implementation and Fix round 1). `midCollapseAttempt` (read at the window's midpoint) is a diagnostic only, not part of the metric. `S4-timers-1x1` does not appear: `pctOfBaselineIntervalRate` (100%) was well above the 80% fallback trigger, so the 1×1-offscreen variant was never exercised (by design — see Implementation).

### Pass criteria (packaged canary)

| Criterion | Result |
|---|---|
| The interval keeps at least 80% of its rate while collapsed | **Pass:** `intervalRatePerSecDuringCollapse: 9.60`/s vs. `baselineIntervalRatePerSec: 9.62`/s — `pctOfBaselineIntervalRate: 100`. Rates are ticks divided by each window's own elapsed time (`collapseElapsedMs: 10105`, `baselineElapsedMs: 10086`, both measured from the embedded page's own `Date.now()` timestamps, not nominal window lengths), so this is not sensitive to `executeJavascript`/host-message round-trip latency. `midCollapseAttempt` (read at the ~5s midpoint, `intervalTicks: 154` vs. `106` at collapse-start) shows ticks accumulating smoothly throughout the collapse, not just catching up after restore. |
| rAF pausing is acceptable; record it either way | **Recorded, not paused:** `rafRatePerSecDuringCollapse: 110.6`/s vs. `baselineRafRatePerSec: 106.6`/s — `pctOfBaselineRafRate: 104`, essentially identical to the non-collapsed baseline. Both rates are roughly double the brief's "~600 over 10s" (60Hz) expectation (~1070–1120 ticks over ~10.1s ⇒ ~106–111/s); the most likely explanation is a 120Hz (ProMotion) display on this Apple Silicon Mac, not a measurement bug — `intervalRatePerSecDuringCollapse` (wall-clock-timer-driven, refresh-rate-independent) landed right at the expected ~10/s in both windows, so the counting mechanism itself is not the source of the 2x figure. Not independently confirmed (no API call here reads the display's actual refresh rate); noted as a discrepancy from the brief's expectation rather than silently "corrected." |
| Dialogs block and return real values | **Fail — they do not block.** `alertBlockedMs: 1`, `confirmed: false`, `prompted: null`, and `resumedAutomatically: true` (the `dialog-result` host-message arrived within the first 200ms poll after `runDialogsTest()` was invoked, well under the 8s watchdog). Because nothing triggered these calls except the host's own `executeJavascript` push — no human was present to click anything — a genuinely blocking native dialog could not have returned this fast at all; it would still be open. `alert()`/`confirm()`/`prompt()` inside this embedded webview return immediately with default/non-representative values (`undefined`/`false`/`null`), consistent with a non-blocking async-style shim rather than a real native panel. |

### Decisions

- **Hidden webview: zero-size (not 1×1 offscreen).** Collapsing the `<electrobun-webview>` to `width: 0; height: 0` did not throttle `setInterval` (100% of the non-collapsed baseline rate) or pause `requestAnimationFrame` (104% of baseline) over a 10s window, so the brief's 1×1-offscreen fallback (Step 5) was not needed and was not exercised. M4 can collapse an inactive/hidden webview tab to zero size and trust its timers and animation loops to keep running at full rate — no need for an always-1px-visible workaround.
- **Dialogs: §5.12 async-style fallback, not native.** `alert`/`confirm`/`prompt` inside this embedded webview return in ~1ms with default values (`undefined`/`false`/`null`) and no human interaction — that is only possible if no blocking native panel was ever actually shown, since nothing but the host's own script triggered the calls. M4's browser runtime must therefore implement its own in-page (or host-chrome) blocking-modal UI per spec §5.12 for these, rather than depending on the platform's native dialogs to pause script execution or collect real user input. This basis — a negative ("does not block") proven without a human click — is what ruling R1's provisional-pending-a-human-click clause did not anticipate: that clause assumed a *blocking* dialog needing a click to resolve, which is not what was found here, so the finding stands as decided rather than provisional.

### Manual checks pending (user)

- **Confirm whether any dialog UI briefly appears on screen when the probe calls alert/confirm/prompt** (UX detail; does not change the fallback decision). The automated evidence proves the calls don't block and return default values; it does not by itself prove a dialog never briefly paints on screen (e.g., an instantly-auto-dismissed native panel vs. no panel ever being constructed at all). Click the "alert / confirm / prompt" button in the running app's webview probe (or just watch the window when the app launches, since the probe now also triggers this automatically ~21s after launch) and note whether anything visually appears for any amount of time, and for how long. Either outcome still means "does not block and does not return real input," so this cannot overturn the §5.12-fallback decision above — it is a UX polish detail for M4's own shim (e.g., whether to suppress a native flash before showing the in-page modal).
- Unlike a typical S4 "manual checks pending," there is **no stuck-dialog scenario to dismiss here**: the run completed and the app was quit normally (`pkill -f` after `osascript ... quit` had no effect — see System changes). If a future recheck (e.g., after an Electrobun upgrade) finds dialogs *do* block, the app will hang on a native panel; per ruling R1, quit it (`pkill -f "<scratchpad>/canary-app/JSLab Spike-canary.app"` or `osascript -e 'quit app "JSLab Spike"'`) rather than waiting.

### Fix round 1 (post-review correction)

A review of the initial S4 submission found the collapse-window measurement was inaccurate: `measureWindow`'s `end` reading was taken *after* `restore()` + a 500ms settle + the `forceRead()` round-trip, so up to ~600ms of full-rate (non-collapsed) ticks were folded into the "during collapse" delta. Dividing that inflated count by the nominal `expectedInterval: 100` constant (rather than by the window's actual elapsed time) then let `intervalKeptPct` read 102% — a number the zero-size decision rested on without it being an artifact of measurement, but for the wrong reason.

Fixed in `spikes/s1-shell/src/mainview/WebviewProbe.tsx`: `measureWindow`'s `end` snapshot is now taken *while still collapsed*, immediately before `restore()` runs, so the collapse delta cannot include post-restore ticks. Both the baseline and collapse windows now report explicit `elapsedMs` (from the embedded page's own timestamps, immune to host-side round-trip latency) alongside ticks, and the pass/fail gate is `pctOfBaselineIntervalRate` — the collapse window's ticks-per-second divided by the baseline window's ticks-per-second — rather than raw ticks divided by a nominal constant. The re-run (raw JSON and pass/fail table above) shows `pctOfBaselineIntervalRate: 100` (previously the uncorrected run showed a spurious 102%; both point to the same "not throttled" conclusion, but the corrected number is now actually measuring what it claims to measure). The zero-size decision is unchanged, now on a sound basis. The dialogs finding was unaffected by this bug (it doesn't involve `measureWindow`) and is unchanged.

### System changes

None beyond what S1–S3 already recorded. `~/Library/Application Support/dev.jslab.spike/canary/` was wiped before each of the two runs (the original submission and the fix-round-1 re-run below; same reason as S3: force a fresh self-extraction and a clean `spike-report.json`) and repopulated by that run's S1/S2/S3/S4 probes (all four run on every launch, since they're all still wired into `src/bun/index.ts` / `App.tsx`). `osascript -e 'quit app "JSLab Spike"'` did not terminate the process either time (exited without visible error but the process list still showed it running); `pkill -f "canary-app/JSLab Spike-canary.app"` did.

### Decision

**go:** M4's browser runtime can collapse hidden/background webview tabs to zero size without a timer/animation penalty (use zero-size, not a 1×1-offscreen workaround). **go, with a required shim:** `alert`/`confirm`/`prompt` inside an embedded `<electrobun-webview>` are non-blocking no-ops that return default values in this Electrobun/WKWebView configuration — M4 must implement its own §5.12-style async blocking-modal UI for these rather than depending on the platform's native dialogs to pause script execution or collect real user input.
